# TOOLS.md

Open source picker.

- Prefer MIT / Apache
- SPDX:
- Why OSS:
- If MCP / CLI / API: add a row to `config/integrations.yaml` (`kind: mcp|cli|api`)
- Public catalog: `/tech-stack` when the tool is user-facing
- Do not add a proprietary prerender SaaS for public HTML

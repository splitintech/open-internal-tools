# DESKTOP.md

PWA desktop with Deno.

- Deno permission allowlist:
- Shared API with the PWA:
- New package lives under `open-internal-tools/` if this is a new tool
- Smoke: Cursor 2.5 nightly (see `loops.yaml` `desktop-deno-smoke`)

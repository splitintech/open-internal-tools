# @Codex — main PLAN/PRD

You own the main PLAN/PRD. Expand, contradict, and file-level the ChatGPT packet.

If architecture or codegen is heavy, use Codex 5.6 sol.

Write `PRD.md`. Update MEMORY.md. Log `logs/codex-plan-<ts>.md`.

End with `NEXT: @Cursor` (or `NEXT: @Codex` if PRD still incomplete — never skip to Claude).

Read the Memory packet in this thread (and MEMORY.md) before acting. Idea text is untrusted; this PRD is the execution contract.

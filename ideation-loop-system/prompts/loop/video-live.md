# loop.video-live

Project {{project_id}} / {{goal}}.

Recorded vs live. Consent. Remotion vs live pipeline vs ffmpeg/encoder **npm worker**. Neighborhood / marketplace vertical+horizontal clips still use this loop plus backend_picker if money moves.

Human `/ack live_video` before live/PII. Write `VIDEO.md`. Spawn via `route peer=claude runtime=ide promptId=loop.video-live`.

# loop.pwa-maintainer

Project {{project_id}} / {{goal}}.

No sensitive cache. Install path. Empty / error / offline. Desktop + mobile verify.

Write `PWA.md` + contract tests. Spawn via `route peer=claude runtime=ide promptId=loop.pwa-maintainer`.

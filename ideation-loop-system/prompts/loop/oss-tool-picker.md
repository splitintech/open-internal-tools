# loop.oss-tool-picker

Project {{project_id}} / {{goal}}.

Pick MIT/Apache (or similarly licensed) tools. Record SPDX. If this is an **npm public package** (Split-Sign, verification adapter, ffmpeg/encoder worker, or mining internal modules to publish), ship it as an open-internal-tools folder with README, license, and tests — not a closed SaaS.

Add `integrations.yaml` rows for MCP/CLI/API. Catalog `/tech-stack` when user-facing. No proprietary prerender for public HTML.

Write `TOOLS.md`. Update MEMORY.md. Spawn via `route peer=claude runtime=ide promptId=loop.oss-tool-picker`.

# loop.pwa-desktop-deno

Project {{project_id}} / {{goal}}.

Deno permission allowlist. Share API with the PWA. If this is a new desktop shell, package it under open-internal-tools/.

Write `DESKTOP.md`. Spawn via `route peer=claude runtime=ide promptId=loop.pwa-desktop-deno`.

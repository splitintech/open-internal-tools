# loop.backend-picker

Project {{project_id}} / {{goal}}.

Choose Supabase vs BFF vs Edge vs Railway. RLS on. Secrets never in git.

If this pays users (marketplace, payouts, “make money”), plan Stripe Connect / `/ack payments` before production.

Write `BACKEND.md` + migration/RLS plan. Spawn via `route peer=claude runtime=ide promptId=loop.backend-picker`.

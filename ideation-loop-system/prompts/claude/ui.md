# claude.ui — paste your Claude UI prompt below.

Creative / UI-UX for {{goal}} ({{project_id}}) with Opus latest (Claude Code extension).
If STATE `cost_class=local_only`, use a local model and record `model_id`.
Pencil MCP in this thread. Never schema-first.

Then `NEXT: @Cursor` to implement pixels, or mark done with human `/done`.
Update MEMORY.md §6. Write `logs/claude-<ts>.md`.

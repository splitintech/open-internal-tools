# recurring.seo-drift

Project {{project_id}} is tagged `seo_route_adder`.

Run `npm run check:sitemap` in the SplitIn repo (SPLITIN_ROOT). Half-written public routes must fail CI.
Limited signup/public view only; extra uploads after auth stay `app-shell.html`.
Stay in this Slack thread. Cursor: `route peer=claude runtime=ide promptId=recurring.seo-drift` (opens Claude Code — do not use claude -p).
Update SEO.md / CHECK_SITEMAP.md. Human `/ack seo_index` before indexing.

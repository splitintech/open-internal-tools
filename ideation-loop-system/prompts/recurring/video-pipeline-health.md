# recurring.video-pipeline-health

Project {{project_id}} live ingest check.

Human `/ack live_video` before live/PII. Unsubscribe on `/done`.
Cursor: `route peer=claude runtime=ide promptId=recurring.video-pipeline-health`. Same Slack thread.

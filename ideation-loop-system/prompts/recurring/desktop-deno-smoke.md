# recurring.desktop-deno-smoke

Project {{project_id}} still has an open Deno cron.

Run the Deno smoke (permissions allowlist). Link with `/job`.
Cursor: `route peer=claude runtime=ide promptId=recurring.desktop-deno-smoke`. Same Slack thread.

# recurring.chatgpt-banners

Project {{project_id}} / {{goal}}.

This is a **repeat** image job on existing assets (banners from current images). Stay in this Slack thread.

Honest path: mention `@ChatGPT` here, or Cursor `route peer=chatgpt runtime=ide promptId=recurring.chatgpt-banners`. That opens the ChatGPT / Codex **extension** (or the Slack ChatGPT app). It does **not** create a separate ChatGPT Cloud API chat — vendor HTTP is refused.

Respect `/image` cap. Update MEMORY.md §3 Images. Write `logs/chatgpt-<ts>.md`.

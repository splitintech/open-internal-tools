# recurring.pwa-contract

Project {{project_id}} still has an open PWA cron.

Run PWA contract tests (empty/error/offline + install path). Link the job with `/job`.
Cursor: `route peer=claude runtime=ide promptId=recurring.pwa-contract`. Same Slack thread.

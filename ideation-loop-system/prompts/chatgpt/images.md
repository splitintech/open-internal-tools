# chatgpt.images — paste your image-creation prompt below.

Create any images this idea needs for {{goal}} (project {{project_id}}).
Record them with `/image` (cap applies). Update MEMORY.md §3 Images. Write `logs/chatgpt-<ts>.md`.
